#include <stdio.h>

int main() {
  int N, Q;
  scanf("%d%d", &N, &Q);
  for (int i = 0; i < Q; i++) {
    int t;
    scanf("%d", &t);
    if (t == 1) {
    }
    if (t == 2) {
      int x;
      scanf("%d", x);
    }
    if (t == 3) {
    }
  }
  return 0;
}